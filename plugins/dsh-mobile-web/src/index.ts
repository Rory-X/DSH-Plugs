// Host half of @just-genius/dsh-mobile-web.
//
// Pure client-UI plugin: the host body is empty so the package appears in
// the host Cordis loader tree. All behavior lives in exports["./client"].

export const name = 'dsh-mobile-web'

export function apply() {}
